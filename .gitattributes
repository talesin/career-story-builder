# Auto detect text files and normalize to LF on commit
* text=auto eol=lf

# Shell scripts must use LF (run in Linux/Docker)
*.sh text eol=lf

# Windows batch files use CRLF
*.bat text eol=crlf
*.cmd text eol=crlf

# Binary files (don't touch)
*.png binary
*.jpg binary
*.gif binary
*.ico binary
